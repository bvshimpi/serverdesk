language: node_js
node_js:
  - "8.11"

branches:
  only:
    - master

addons:
    chrome: stable

before_script:
  - npm install -g @angular/cli

cache:
    directories:
        - node_modules

script:
  - ng build --prod --aot

deploy:
  provider: pages
  skip-cleanup: true
  keep-history: true
  github-token: 68464ab3fcdf9acaac8b7bab6fcc17f747d05b2a
  local-dir: ./dist/
  on:
    branch: master